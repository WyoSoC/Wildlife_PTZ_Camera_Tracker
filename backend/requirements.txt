# Eagle Tracker — backend dependencies
#
# PyTorch is NOT listed here because the correct wheel varies by hardware.
# Run the installer instead of pip-installing this file directly:
#
#   python install.py
#
# The installer detects your platform (Jetson / NVIDIA GPU / macOS / RPi / CPU),
# installs the right torch variant, then installs everything below.

# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.0

# ── WebRTC (video streaming to browser) ───────────────────────────────────────
aiortc>=1.9
av>=12.0

# ── Computer vision ────────────────────────────────────────────────────────────
numpy>=1.26
# Pin headless before ultralytics so pip does not pull in opencv-python (GUI),
# which conflicts with the av dylib on macOS.
opencv-python-headless>=4.9
ultralytics>=8.2
deep-sort-realtime>=1.3

# ── NDI SDK Python bindings ────────────────────────────────────────────────────
# Optional — required only for NDI cameras (BirdDog, Bolin).
# Requires the NDI Advanced SDK to be installed first:
#   https://ndi.video/tools/ndi-sdk/
# Then install the Python bindings:
ndi-python>=6.3

# ── Utilities ──────────────────────────────────────────────────────────────────
websockets>=12.0
huggingface_hub>=0.23

# ── System metrics ─────────────────────────────────────────────────────────────
psutil>=5.9
# NVIDIA GPU metrics — optional; graceful no-op when absent
nvidia-ml-py>=12.0
