# NVIDIA GPU — desktop / laptop (CUDA 12.4)
#
# Usage:
#   pip install -r requirements-cuda.txt
#
# This file installs PyTorch with CUDA 12.4 support first, then the rest of
# the dependencies.  The --index-url applies only to the torch packages listed
# in this file; remaining deps come from PyPI as usual.
#
# Supported hardware:
#   - Any NVIDIA GPU with CUDA Compute Capability 6.0+ (Pascal and newer)
#   - Tested on RTX 3090/4090, A100, L4, T4
#
# For CUDA 11.8 (older drivers):
#   Replace cu124 with cu118 in the --index-url and version suffix.

--index-url https://download.pytorch.org/whl/cu124
torch>=2.3.0+cu124
torchvision>=0.18.0+cu124

# All other dependencies from PyPI
--extra-index-url https://pypi.org/simple
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.0
aiortc>=1.9
av>=12.0
opencv-python-headless>=4.9
numpy>=1.26
ultralytics>=8.2
deep-sort-realtime>=1.3
websockets>=12.0
pygame>=2.5
